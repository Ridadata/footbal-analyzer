# Legacy Prototypes

Early exploratory scripts written before the project settled on the FastAPI
architecture in `src/`. They are **not used by the running web app** — kept
here for reference only.

Run one directly from the `football_live_match/` project root, e.g.:

```bash
python -m legacy.api_test
```
